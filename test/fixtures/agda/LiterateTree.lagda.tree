\title{Literate Tree}
\p{A simple literate Agda module using the \code{.lagda.tree} format.}

\agda{
module LiterateTree where

data Nat : Set where
  zero : Nat
  suc  : Nat → Nat

add : Nat → Nat → Nat
add zero    m = m
add (suc n) m = suc (add n m)

-- Unsolved hole
test : Nat
test = {!!}
}
