# 0.5.0 Release Checklist

## Scope and parity

- [x] Confirm full parity against Agda `Agda.Interaction.Base` command constructors.
- [x] Verify `src/protocol/command-registry.ts` has all upstream commands and no planned entries.
- [x] Confirm property tests assert parity (`getPlannedProtocolCommands()` is empty).

## Build and tests

- [x] Run `npm run build`.
- [x] Run `npm test`.
- [x] Optionally run live Agda integration: `RUN_AGDA_INTEGRATION=1 npm run test:integration`.
- [x] Optionally run backend integration: `RUN_AGDA_BACKEND_INTEGRATION=1 AGDA_BACKEND_EXPR=GHC npm run test:integration`.

## Documentation and metadata

- [x] Verify `package.json` version is `0.5.0`.
- [x] Verify MCP server version in `src/index.ts` is `0.5.0`.
- [x] Confirm README tool reference includes backend and display/highlighting tools.
- [x] Confirm CHANGELOG contains the `0.5.0` release entry.

## Packaging and release

- [x] Run `npm run verify`.
- [x] Review package contents with `npm pack --dry-run` output.
- [ ] Create release commit using `docs/releases/0.5.0-commit-message.txt`.
- [ ] Tag and publish according to project release process.
